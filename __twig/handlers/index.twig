{% extends "base.twig" %}

{% block content %}
    {% for snippet in site_config.snippets.front_page_hero %}
        {% include "#{snippet.filesystem_location}.twig" with {'snippet': snippet} %}
    {% endfor %}

    <div class="custom-container">
        <div class="row border-bottom2">
            <div class="col-md-8 col-sm-8 style-box1 tzcontent">
                {{ include("_front_page.twig")  }}
            </div>
            {{ include('_sidebar.twig') }}
        </div>
    </div>
{% endblock %}